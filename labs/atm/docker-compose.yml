name: atm-cyber-range-vulnerable

services:
  atm-lab:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: atm-lab-vulnerable
    ports:
      - "8080:8080"
    environment:
      - SECURE_MODE=false
      - DB_PATH=/app/data/atm.db
    volumes:
      - atm_data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8080/api/v1/mode')\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  atm_data:
